<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
<title>修改密碼</title>
</head>
<body>
	<div align="center">
		<h1 align="center">修改密碼</h1>

		<a th:href="@{/movies/index}">回首頁</a> | <a th:href="@{/movies/create}">新增電影</a>
		| <a th:href="@{/movies/upload}">上傳檔案</a> | <a th:href="@{/logout}">登出</a>

		<form th:action="@{/changePassword}" method="post">
			<input type="password" name="oldPassword" placeholder="原密碼" required />
			<br> <input type="password" name="newPassword" placeholder="新密碼"
				required /> <br> <input type="password" name="confirmPassword"
				placeholder="確認新密碼" required /> <br>
			<button type="submit">修改密碼</button>

			<p style="color: red;" th:if="${error}" th:text="${error}"></p>
			<p style="color: green;" th:if="${success}" th:text="${success}"></p>
		</form>
	</div>
</body>
</html>
